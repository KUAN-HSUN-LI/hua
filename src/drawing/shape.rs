#[allow(dead_code)]
pub struct Rect(pub u32, pub u32, pub u32, pub u32);
#[allow(dead_code)]
pub struct Circle {
    center: (u32, u32),
    radius: u32,
}

#[allow(dead_code)]
pub struct Triangle {
    x0: u32,
    y0: u32,
    x1: u32,
    y1: u32,
    x2: u32,
    y2: u32,
}

#[allow(dead_code)]
pub struct Ploygon {}
